# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'export_frame' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Vigram', :git => 'https://github.com/vigram-sw/SDK_iOS_viDoc_Distribution.git', :tag => '1.0.6'

end
